// import PropTypes from 'prop-types';
import css from './UserBox.module.css';

export const UserBox = () => {
  return <div className={css.userBox}></div>;
};

// UserBox.propTypes = {
//   cards: PropTypes.array,
// };
